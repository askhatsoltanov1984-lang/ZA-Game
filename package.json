{
  "name": "za-card-game",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "node --test test/*.test.js",
    "check": "node --check server.js && node --check room-store.js && node test/check-client.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.7.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "playwright": "^1.62.1",
    "socket.io-client": "^4.8.3"
  },
  "engines": {
    "node": ">=24 <25"
  },
  "overrides": {
    "qs": "^6.16.0"
  }
}
