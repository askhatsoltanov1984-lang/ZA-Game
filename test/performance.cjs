const {chromium}=require('playwright');const fs=require('node:fs');
(async()=>{const browser=await chromium.launch();try{const page=await browser.newPage({viewport:{width:390,height:844}});const cdp=await page.context().newCDPSession(page);await cdp.send('Network.enable');await cdp.send('Network.emulateNetworkConditions',{offline:false,latency:150,downloadThroughput:200000,uploadThroughput:100000});await cdp.send('Emulation.setCPUThrottlingRate',{rate:4});const start=Date.now();await page.goto('http://127.0.0.1:3187');await page.waitForLoadState('networkidle');const loadMs=Date.now()-start;await page.evaluate(()=>{state.yourHand=['4s','4h','4d','4c','5s','5h','5d','5c','6s','6h','6d','6c','7s','7h','7d','7c','JOK1','JOK2'];showScreen('game-screen');renderHand();});const duration=await page.evaluate(()=>{const t=performance.now();for(let i=0;i<100;i++)document.querySelector('#hand-cards .card').click();return performance.now()-t;});const metrics={browser:browser.version(),localServer:true,latencyMs:150,downloadBytesPerSecond:200000,cpuSlowdown:4,loadToNetworkIdleMs:loadMs,oneHundredSelectionTogglesMs:duration,scope:'Synthetic desktop throttling, not a physical midrange phone; load includes external fonts'};fs.writeFileSync('../../outputs/performance.json',JSON.stringify(metrics,null,2));console.log(metrics);}finally{await browser.close();}})();
